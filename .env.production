# Production build overrides (loaded automatically by `vite build`).
# Only VITE_* vars here are public (baked into the client bundle).
# Server secrets (STRIPE_*, SUPABASE_SERVICE_ROLE_KEY, ...) are NOT here —
# they are set as Cloudflare Worker secrets via `wrangler secret put`.

VITE_APP_URL=https://prosimport.com
